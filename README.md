# enea.work Clone

A simple static clone of enea.work.

## Usage

Just open `index.html` in your browser. That's it.

## Files

- `index.html` - Main HTML file
- `styles.css` - Styles

## Development

Edit `index.html` and `styles.css` directly. Refresh your browser to see changes.
